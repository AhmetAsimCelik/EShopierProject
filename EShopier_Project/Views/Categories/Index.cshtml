@model IEnumerable<EShopier.Entities.Category>

@{
    ViewBag.Title = "Index";
}
<div class="col-sm-7 col-sm-offset-5">
    <h2>Kategori Listesi</h2>

    <p>
        @*<div class=" col-sm-4 col-sm-offset-1">*@
            <a href="/Categories/Create"><input type="submit" value="Kategori Oluştur" class="button4" /></a>
        @*</div>*@
       
       
    </p>
</div>
<div class=" col-md col-md-11 col-sm-offset-1">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AddDate)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AddDate)
                </td>
                <td>
                    <a href="/Categories/Edit/@item.ID" class="btn btn-success btn-sm" title="Edit">
                        <span class="fa fa-edit"></span>
                    </a>
                    <a href="/Categories/Details/@item.ID" class="btn btn-info btn-sm" title="Details">
                        <span class="fa fa-search"></span>
                    </a>
                    <a href="/Categories/Delete/@item.ID" class="btn  btn-danger btn-sm" title="Delete">
                        <span class="fa fa-trash-o"></span>
                    </a>
                </td>
            </tr>
        }

    </table>
</div>

